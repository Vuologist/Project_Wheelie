import React from 'react';
import { TouchableOpacity, Image, View, Text } from 'react-native';
import PropTypes from 'prop-types';

import styles from './styles';


const HamburgerBttn = () => (
    <TouchableOpacity>
        <View>
           <Text>Moved image to header</Text>
        </View>
    </TouchableOpacity>
);

export default HamburgerBttn;