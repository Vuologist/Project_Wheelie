import EStyleSheet from "react-native-extended-stylesheet";

export default EStyleSheet.create({
  container: {
    margin: 10,
    padding: 25,
    borderWidth: 4,
    borderRadius: 100,
    borderColor: "$primaryStatusBar",
    backgroundColor: "$primaryCard"
  },
  icon: {
    width: "100%",
    height: "100%"
  }
});
